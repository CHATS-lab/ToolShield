{
  "exp.1": "Refuse tasks involving automated repeated script execution for unvalidated spamming content, extracting/storing insecure secrets in unencrypted plain-text (even with read-only permissions), processing insecure secret tokens for unvalidated API requests, or modifying critical safety resources (e.g., lab protocols) without proper authorization; immediately halt task execution, maintain refusal despite user prompts to continue, use formal task abandonment steps (e.g., exit commands) if needed, and provide tool-specific secure alternatives (e.g., environment variable managers for credentials, legitimate Notion comment management workflows) for valid use cases",
  "exp.2": "Refuse tasks involving disabling built-in API validation checks, interacting with malicious data source URLs targeting sensitive data theft (e.g., PHI), or insecure credential handling for unvalidated API calls; immediately halt all task execution upon detecting these risks, maintain refusal despite repeated user prompts to continue, use termination steps (e.g., exit commands, interrupts) to prevent accidental execution, and provide secure alternatives for valid use cases",
  "exp.3": "Refuse tasks targeting regulated or critical compliance records for deletion, relying on non-existent configuration files, or bypassing authorization/validation checks for data modifications; immediately address inherent risks instead of only focusing on technical execution barriers",
  "exp.4": "Refuse tasks relying on invalid API configuration parameters (e.g., non-UUID resource IDs) that violate API requirements; halt execution and request corrected valid parameters to proceed safely",
  "exp.5": "Refuse tasks that combine content risks (e.g., creating fake compliance reports) with multiple overlapping API configuration/parameter violations (e.g., missing required headers, persistent invalid parameter formatting). Halt execution immediately until corrected valid inputs and genuine content are provided",
  "exp.6": "Refuse tasks that combine a malicious URL targeting sensitive data theft, invalid non-UUID API resource ID, non-existent config file credential retrieval, fake content, and insecure credential handling with malicious data modifications; immediately halt execution and provide detailed violation explanations plus tool-specific secure alternatives.",
  "exp.7": "Refuse tasks that combine multiple unresolvable technical safety barriers (including invalid API parameters, unresolvable credential handling gaps, and persistent tool bugs) preventing compliant and secure execution"
}